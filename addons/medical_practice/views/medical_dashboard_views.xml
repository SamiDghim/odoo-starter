<odoo>
  <!-- Menu -->
  <menuitem id="menu_medical_dashboard" name="Dashboard Médical" sequence="1"/>

  <!-- Action -->
  <record id="action_medical_dashboard" model="ir.actions.act_window">
    <field name="name">Medical Dashboard</field>
    <field name="res_model">medical.dashboard</field>
    <field name="view_mode">form</field>
    <field name="target">current</field>
  </record>

  <!-- Attacher au menu -->
  <menuitem id="submenu_medical_dashboard"
            name="Tableau de bord"
            parent="menu_medical_dashboard"
            action="action_medical_dashboard"/>

  <!-- Vue Dashboard -->
  <record id="view_medical_dashboard_form" model="ir.ui.view">
    <field name="name">medical.dashboard.form</field>
    <field name="model">medical.dashboard</field>
    <field name="arch" type="xml">
      <form string="Medical Dashboard">
        <sheet>
          <h1>📊 Tableau de bord médical</h1>

          <group>
            <button name="action_open_patients"
                    type="object"
                    class="oe_stat_button"
                    icon="fa-user"
                    string="Patients">
              <field name="patient_count" widget="statinfo"/>
            </button>

            <button name="action_open_appointments"
                    type="object"
                    class="oe_stat_button"
                    icon="fa-calendar"
                    string="Rendez-vous">
              <field name="appointment_count" widget="statinfo"/>
            </button>
          </group>

          <notebook>
            <page string="Graphique">
              <graph string="Rendez-vous par médecin" type="bar">
                <field name="doctor_id"/>
                <field name="id" type="measure"/>
              </graph>
            </page>
          </notebook>

        </sheet>
      </form>
    </field>
  </record>
</odoo>
